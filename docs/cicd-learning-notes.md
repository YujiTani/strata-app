# GitHub Actions CI/CD 導入で学んだこと

## 背景

Fly.io への手動デプロイ（`fly deploy`）が成功した後、roadmap.md が定義する Week 1 の完了条件
「push で自動デプロイされる」を満たすため、GitHub Actions の導入を検討した。
実装コードを渡してもらうのではなく、設計判断を対話しながら自分でワークフローファイルを書く、という進め方をとった。

---

## 1. CI/CDの「トリガー」とは何か

GitHub Actions の `on:` セクションで「どのイベントでワークフローが起動するか」を定義する設定。
push、pull_request、タグ作成、スケジュール実行など複数のイベント種別がある。

### 一般的な3段階のパターン

| 段階 | 構成 | 向いている規模 |
| --- | --- | --- |
| ① 最小構成 | `push to main` → build → deploy を1本化 | 個人開発・小規模 |
| ② CI/CD分離 | lint/test/buildのCIジョブ（PR時にも実行）と、mainへのpush時だけ動くdeployジョブを分離 | チーム開発 |
| ③ 成熟構成 | ステージング自動デプロイ＋本番は手動承認、タグ/リリースベースのデプロイ、Blue-Green等 | 企業レベル |

自分のプロジェクトはソロ開発でPRフローも使っていないため、①が身の丈に合っていると判断した。

### AI時代で何が変わった/変わっていないか

- **変わった**: ワークフローYAMLを書く・デバッグする速度（Claude Codeとの対話で今回体感）。
  Fly.io自体の進化として `flyctl deploy --remote-only` を使えば、GitHub Actions runner上でDockerビルドをせず
  Fly側のリモートビルダーに任せられ、レイヤーキャッシュも永続化されて速い（これは「AI」というよりFlyのアーキテクチャの恩恵）。
- **変わっていない**: トリガー設計の思想そのもの（push/PR/tag/schedule駆動）はCI/CD黎明期から不変。
  「AIネイティブなCI/CD」という別パラダイムがあるわけではなく、過大評価しないよう注意した。

---

## 2. 今回の設計判断（3点）

| 論点 | 決定 | 理由 |
|---|---|---|
| トリガー範囲 | `master` への push のみ、lint→deployを1ジョブに一本化 | PRフローを使っていない現状に対し、CI/CDジョブ分離はオーバースペック |
| ビルド方式 | `flyctl deploy --remote-only` | Fly側リモートビルダーでビルド。fly.toml の設定とそのまま整合し、runnerのジョブも軽量化 |
| flyctl導入 | 公式 `superfly/flyctl-actions` | 自前でinstallスクリプトを管理するより、公式サポートの範囲で完結させる |

既存の `.husky/pre-push` の `biome check` はローカルの利便性フックに過ぎず `--no-verify` で回避可能なため、
CI側でも同じ `bun run lint` を再実行し「本当のゲート」とする方針にした。

---

## 3. GitHub Secretsの管理階層

| 階層 | スコープ |
| --- | --- |
| Repository secrets | 単一リポジトリのみ |
| Environment secrets | リポジトリ内の特定Environment（承認フローやブランチ制限を紐づけ可能） |
| Organization secrets | Organization配下の複数リポジトリで共有 |

`gh secret set FLY_API_TOKEN --repo <owner>/<repo>` はデフォルトで Repository secrets に書き込まれる。
個人アカウント・単一リポジトリ構成では、影響範囲を最小化する意味でこの粒度が適切（最小権限の原則）。

### Secretsの値はCLI/APIからも読み出せない

GitHub Secretsは書き込み専用・暗号化保存の設計で、GitHub自身も平文を保持しない。
`gh secret list` で確認できるのは「存在するか」「最終更新日時」までで、値そのものは確認不可能。
正しい値かどうかの厳密な検証は、実際にワークフローを走らせて認証が通るかで確認するしかない。

---

## 4. トークンのスコープ設計（見落としがちな重要ポイント）

Repository secretの粒度そのものより本質的なリスクは、**発行したトークン自体のスコープ**だった。

- `flyctl tokens create deploy -a <app名>`: **そのFlyアプリ専用**にスコープされたデプロイトークン
- `flyctl auth token` 等: アプリ指定なし＝**アカウント全体**に効く広い権限のトークン

今回は前者（アプリ専用トークン）を使用。仮にリポジトリのActionsが（サプライチェーン攻撃や設定ミスで）
漏洩したとしても、被害範囲を「このアプリだけ」に限定できる。Repository secret vs Organization secretという
置き場所の議論より、この「発行するトークン自体の権限を絞る」判断の方が実質的な影響が大きい。

---

## 5. サードパーティActionのセキュリティ（SHA固定 vs ブランチ参照）

`uses: superfly/flyctl-actions/setup-flyctl@master` のように**ブランチ名**で参照すると、
実行のたびに相手リポジトリの「今この瞬間の最新コミット」を取得して実行する。
自分のリポジトリのコードを一切変更していなくても、相手側のコードが変われば、
次回のデプロイで動く中身が勝手に変わってしまう。

### 具体的な攻撃シナリオ

1. 参照先リポジトリのメンテナアカウントが乗っ取られる、または悪意あるPRがmasterにマージされる
2. 環境変数を外部に送信するコードが仕込まれる
3. 次回の自分のpush時、CIがそのコードを実行し `secrets.FLY_API_TOKEN` が抜き取られる

これは実在するリスクで、2025年3月の `tj-actions/changed-files` 乗っ取り事件のような
サプライチェーン攻撃として知られている。

### 対策

コミットSHA（不変の参照）で固定する。

```yaml
# NG: 動く参照。相手の都合で中身が変わる
- uses: superfly/flyctl-actions/setup-flyctl@master

# OK: 不変の参照。確認した時点のコードで固定される
- uses: superfly/flyctl-actions/setup-flyctl@<40文字のコミットSHA>
```

トレードオフとして自動更新はされなくなるが、Dependabotの設定でSHA固定のActionでも
更新PRを自動生成でき、「更新は来るが中身は毎回差分としてレビューできる」形にできる。

---

## 6. push即デプロイのリスクとどこまで対策するか

「pushしたら問答無用でデプロイされる」ことへの懸念から、以下の選択肢を比較検討した。

| 選択肢 | 内容 | コスト |
| --- | --- | --- |
| ① lintゲートのみ | 今回採用。lint失敗ならdeployされない | 追加コストなし。ただしロジックバグは防げない |
| ② Environment手動承認ゲート | production環境へのデプロイ前にGitHub UI上で承認が必要 | 数行の追加で実現可、ただし「自動」感が薄れる |
| ③ ステージング環境+手動昇格 | 別Flyアプリに先にデプロイし、確認後に本番へ昇格 | 本番相当で確認できるが、アプリ・DB二重管理のコスト大 |
| ④ PRベース+branch protection | 標準的手法だが今回は対象外 | — |

**結論**: ①を採用。理由は以下の2点。

- 個人開発の規模では、機能追加のたびに目視チェックすれば複雑に絡み合うバグは起きにくいと判断
- 現状 `package.json` にまだ `test` スクリプトが存在しない（Vitest導入はroadmap上Week 3以降）。
  そのため②③を選んでも「ロジックバグを機械的に検出して止める」ことは今の時点ではできず、
  どの構成でも人間の目視確認に依存する制約は同じ。速度を優先する判断の方が合理的と判断した。

---

## 7. YAML基礎構文の再確認

- `key: value` が基本単位。コロンの後は半角スペース必須
- **インデント（スペースの数）だけで入れ子構造を表現**する（`{}` のような括弧はない）

  ```yaml
  on:
    push:
      branches: [master]
  ```

- インデントは半角スペースのみ、**タブ禁止**。同じ階層は同じスペース数で揃える
- 配列はインライン `[master]` かブロック（`- master` の羅列）の2種類
- コメントは `#`

---

## 8. `steps:` の中で何度もハマった「`-` の意味」

実装フェーズで一番繰り返しつまずいたのがこのルール。

`steps:` の中身は「ステップの配列（リスト）」であり、**`-` は「新しいステップ（配列の要素）がここから始まる」という意味**を持つ。

```yaml
steps:
  - uses: actions/checkout@v4      # 1つ目のステップ
  - name: Install dependencies     # 2つ目のステップの開始
    run: bun install --frozen-lockfile   # ← これは新しいステップではなく、
                                          #   直前の`- name:`と同じステップの2つ目のキー
```

同じステップの中に複数のキー（`name:` と `run:`、`name:` と `uses:`）を書くときは、**最初のキーだけに `-` を付け、2つ目以降のキーには `-` を付けずに、1つ目のキーの文字が始まる位置にインデントを揃える**。

この理解が曖昧だと、以下のような壊れ方をする:

```yaml
# NG例1: 2つ目のキーにも`-`を付けてしまう
- name: Deploy to fly.io
- run: flyctl deploy --remote-only
# → 「name」と「run」が別々の2つのステップとして解釈され、
#   nameだけのステップとrunだけのステップに分裂する

# NG例2: ステップ全体のインデントが浅く、上位階層と混同する
jobs:
  deploy:
    runs-on: ubuntu-latest
  steps:              # ← deployと同じ階層になっている
    - uses: ...        # → jobsの中にdeployとstepsという2つの別ジョブがある、
                        #   という誤った構造になり、YAMLパースエラーになる
```

いずれもYAMLとして構文エラーになるか、意図と違う構造に解釈される。`ruby -ryaml -e "YAML.load_file(...)"` のようなパーサーで都度検証すると、エラーメッセージで即座に気づける。

---

## 9. `uses:` と `run:` の役割の違い

| 書き方 | 意味 |
| --- | --- |
| `uses:` | 誰かが作った再利用可能な処理（Action）を呼び出す。関数呼び出しに近い |
| `run:` | シェルコマンドを直接実行する |

### `oven-sh/setup-bun` と `bun install --frozen-lockfile` は別物

最初、この2つを混同しかけた。役割が完全に異なる。

- **`oven-sh/setup-bun`**（`uses:`）: まっさらな仮想マシンに **Bunという実行環境自体をインストールする**ステップ。これがないと `bun` コマンドがそもそも存在しない
- **`bun install --frozen-lockfile`**（`run:`）: すでにインストールされた `bun` コマンドを使って、**このプロジェクトの依存パッケージ**を揃えるコマンド

新品のPCに「Node.js/Bunそのもの」をインストールする作業と、その上でプロジェクトフォルダに移動して依存関係をインストールする作業、という2段階に分かれている。

### `run:` の中身は極力スクリプト経由で呼ぶ

`bun run lint` のステップを書くとき、`package.json` の `scripts.lint`（`biome check .`）の中身をそのままコピーして書きそうになったが、それだと同じコマンドがワークフローと `package.json` の2箇所に重複し、将来の変更漏れリスクが生まれる。`bun run lint` のように**スクリプト名を呼び出す**形にすることで、ローカルとCIで完全に同じコマンドを実行しつつ、情報源を1箇所に保てる（DRY原則）。

---

## 10. SHA固定に関する2つの誤解と訂正

サードパーティAction（`superfly/flyctl-actions`）をSHA固定する際、2つ誤解していた点があった。

### 誤解1: 「固定バージョン（タグ）が存在しないからSHAを使う」

実際に確認すると、`superfly/flyctl-actions` には `v1` や `1.5` のようなタグが普通に存在していた。

``` bash
$ gh api repos/superfly/flyctl-actions/tags --jq '.[].name'
v1.4, v1, 1.6, 1.5, ...
```

**正しい理由**: タグも技術的には「動く可能性のある参照」（リポジトリ所有者が意図的に、あるいはアカウント乗っ取りで指す先を書き換えられる）。`superfly` はGitHub公式ではなく一企業のため、タグよりさらに確実な「絶対に動かないSHA」を選ぶ、という判断。「タグがないから仕方なく」ではなく「タグより確実だから」SHAを選んだ。

### 誤解2: 「SHAで固定しているのはflyctl CLI自体のバージョン」

これも誤り。`setup-flyctl` の中身（`action.yml`）を確認すると、2つの独立したバージョン軸があることが分かった。

| 何のバージョン？ | どこで指定する？ | SHA固定した対象か |
| --- | --- | --- |
| インストーラー（Actionスクリプト）自体 | `uses: .../setup-flyctl@<ここ>` | ✅ 固定した |
| インストールされるflyctl CLI本体 | `with: version: '0.3.xxx'`（デフォルト`latest`） | ❌ 今回は`latest`のまま |

SHA固定していたのは「インストール作業を行うコードが改ざんされないようにする」というサプライチェーン対策であり、flyctl CLI自体のバージョン管理とは別の話だった。flyctl CLI本体は今回 `latest` のままにする判断をした（個人開発の規模でリスクとリターンを見て許容）。

---

## 11. ワークフロー内でGitHub Secretsを使う方法

```yaml
- name: Deploy to fly.io
  run: flyctl deploy --remote-only
  env:
    FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
```

- `${{ ... }}` はGitHub Actions独自の式（Expression）構文。実行時に評価されて値が埋め込まれる
- `secrets.FLY_API_TOKEN` は、リポジトリのSecretsに登録した値を参照する
- `env:` は**このステップだけ**に書いており、ジョブ全体（`jobs.deploy.env`）には書いていない。これは「シークレットに触れる必要のあるステップだけに絞ってアクセスを許可する」というスコープ最小化の実践（4.のトークンスコープ設計と同じ思想を、ワークフロー内の各ステップ単位でも適用した）
