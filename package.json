{
	"name": "strata-app",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"devDependencies": {
		"@biomejs/biome": "^2.5.0",
		"@types/bun": "latest",
		"husky": "^9.1.7",
		"typescript": "^5.8.0"
	},
	"scripts": {
		"lint": "biome check .",
		"format": "biome format --write .",
		"prepare": "husky",
		"typecheck": "tsc --noEmit && tsc --noEmit -p apps/client"
	}
}
